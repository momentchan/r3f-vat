uniform float uTime;
uniform float uSeed;
uniform float uManual;

varying vec2 vUv;
varying vec2 vUv2;

void main() {
  // Base color - can be set via material properties
  vec4 color = vec4(0.8, 0.8, 0.9, 1.0);

  // csm_DiffuseColor = color;
}
